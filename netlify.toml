[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["jspdf"]

[dev]
  command = "npm run dev"
  functions = "netlify/functions"
  targetPort = 5173
